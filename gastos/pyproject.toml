[project]
name = "gastos"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Galbfran", email = "francogalbiati984@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
gastos = "gastos:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.2",
]
